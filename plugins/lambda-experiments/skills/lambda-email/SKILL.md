---
name: lambda-email
description: Compose and send an HTML email summary for a Lambda experiment, including charts. Use when asked to email or report results from a Lambda run.
---

Read `../../commands/lambda-email.md` completely and follow it as the canonical workflow. Treat an optional state-file path in the user's request as the command argument, ignore Claude-only frontmatter, and preserve Codex approval requirements for sending email.
