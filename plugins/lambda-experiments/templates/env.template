# Lambda instance .env template
# Plain KEY=VALUE format (no `export` prefix) for maximum compatibility
# Source with: set -a; source .env; set +a
#
# Copy this to the Lambda instance via:
#   scp .env ubuntu@IP:~/control-arena/.env

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
HF_TOKEN=
LAMBDA_API_KEY=
OPENROUTER_API_KEY=
