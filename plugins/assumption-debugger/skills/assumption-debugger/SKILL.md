---
name: assumption-debugger
description: Diagnose a bug with a hypothesis-first process before changing code. Use when behavior is wrong, something stopped working, or the user asks for systematic debugging.
---

Read `../../agents/assumption-debugger.md` completely and apply its methodology in the current Codex thread.

Ignore Claude-only agent frontmatter (`model`, `color`, and `tools`). Ask the user conversationally where the document names `AskUserQuestion`. Do not implement a fix unless the user asks for one; diagnosis and implementation remain separate scopes.
